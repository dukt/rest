<?php

/**
 * To customize providers' `scope` and `authorizationOptions`, create a `craft/config/rest.php` file and adjust the config variables to your needs.
 *
 * (Example with Google below)
 */

return array(

    /**
     * Google Scope
     *
     * @see https://developers.google.com/identity/protocols/googlescopes
     */
    // 'googleScope' => [
    //     'https://www.googleapis.com/auth/youtube', // Permission for YouTube
    // ],

    /**
     * Google Authorization Options
     */
    // 'googleAuthorizationOptions' => [
    //     'access_type' => 'offline', // Offline access
    //     'approval_prompt' => 'force', // Force prompt to make sure we get a non-expiring token (refreshable)
    // ],
);
